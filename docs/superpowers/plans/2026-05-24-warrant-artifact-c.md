# Warrant — Artifact C: Shareable Starter Kit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the repo self-contained for new users: `git clone` → `make install-api` → `warrant init` → `warrant run --direction "..."` in under 10 minutes, no private book corpus needed.

**Architecture:** Add a `warrant init` CLI subcommand that scaffolds `.warrant/api-config.json` interactively; add a `sample-library/` directory with 15 hand-crafted engineering principles and a pre-built Librarian index committed to git; add a `Makefile` and `README.md` as the project face. All changes are additive — the Librarian, Agent, and loop packages are untouched.

**Tech Stack:** Python 3.11+ stdlib only for `warrant init`; `sentence-transformers` + numpy for building the sample index (one-time dev step); Makefile (POSIX); Markdown.

---

## File map

| Action | Path |
|---|---|
| Modify | `loop/loop/api/__main__.py` — add `init` subcommand, fix default config path |
| Create | `loop/tests/test_api_init.py` |
| Create | `sample-library/principles.json` |
| Create | `sample-library/build_index.py` |
| Create | `sample-library/index/` (generated by build script, committed) |
| Create | `sample-library/demo-config.json` |
| Create | `Makefile` |
| Create | `README.md` |

---

## Task 1: `warrant init` subcommand

**Files:**
- Modify: `loop/loop/api/__main__.py`
- Create: `loop/tests/test_api_init.py`

### Context

`loop/loop/api/__main__.py` currently has `run` and `resume` subcommands. `_default_config()` wrongly returns `.warrant/config.json` (the skill config path); the `warrant` binary should default to `.warrant/api-config.json`. We fix the default and add `init`.

`ApiConfig` lives in `loop/loop/api/factory.py`. It has these required fields with no defaults: `index_path`, `out_dir`, `base_repo`. `warrant init` writes only those three to JSON; all other fields use dataclass defaults at runtime.

### Steps

- [ ] **Step 1: Write the failing tests**

Create `loop/tests/test_api_init.py`:

```python
from __future__ import annotations
import json
from pathlib import Path
from unittest.mock import patch

import pytest

import loop.api.__main__ as m


def test_init_writes_correct_json(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=[".", "sample-library/index", ".warrant/runs"]):
        import argparse
        m.cmd_init(argparse.Namespace())
    data = json.loads(out_path.read_text())
    assert data == {
        "index_path": "sample-library/index",
        "base_repo": ".",
        "out_dir": ".warrant/runs",
    }


def test_init_uses_defaults_on_blank_input(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=["", "", ""]):
        import argparse
        m.cmd_init(argparse.Namespace())
    data = json.loads(out_path.read_text())
    assert data["base_repo"] == "."
    assert data["index_path"] == "sample-library/index"
    assert data["out_dir"] == ".warrant/runs"


def test_init_uses_custom_values(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=["/my/repo", "/my/index", "/my/runs"]):
        import argparse
        m.cmd_init(argparse.Namespace())
    data = json.loads(out_path.read_text())
    assert data["base_repo"] == "/my/repo"
    assert data["index_path"] == "/my/index"
    assert data["out_dir"] == "/my/runs"


def test_init_declines_overwrite(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    out_path.parent.mkdir(parents=True)
    out_path.write_text('{"base_repo": "original"}')
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", return_value="n"):
        import argparse
        m.cmd_init(argparse.Namespace())
    assert json.loads(out_path.read_text())["base_repo"] == "original"


def test_init_overwrites_on_y(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    out_path.parent.mkdir(parents=True)
    out_path.write_text('{"base_repo": "original"}')
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=["y", "/new/repo", "/new/index", "/new/runs"]):
        import argparse
        m.cmd_init(argparse.Namespace())
    assert json.loads(out_path.read_text())["base_repo"] == "/new/repo"


def test_init_creates_warrant_dir(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    assert not out_path.parent.exists()
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=[".", "sample-library/index", ".warrant/runs"]):
        import argparse
        m.cmd_init(argparse.Namespace())
    assert out_path.parent.is_dir()


def test_main_init_subcommand_routes_to_cmd_init(tmp_path):
    out_path = tmp_path / ".warrant" / "api-config.json"
    with patch.object(m, "_INIT_OUTPUT", out_path), \
         patch("builtins.input", side_effect=[".", "sample-library/index", ".warrant/runs"]), \
         patch("sys.argv", ["warrant", "init"]):
        m.main()
    assert out_path.exists()


def test_default_config_path_is_api_config():
    assert str(m._DEFAULT_CONFIG) == ".warrant/api-config.json"
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd /path/to/warrant
python -m pytest loop/tests/test_api_init.py -v
```

Expected: most tests fail with `AttributeError: module 'loop.api.__main__' has no attribute '_INIT_OUTPUT'` or similar.

- [ ] **Step 3: Implement `warrant init` in `__main__.py`**

Replace the full contents of `loop/loop/api/__main__.py` with:

```python
from __future__ import annotations
import argparse
import json
import sys
from pathlib import Path

from .. import runstore
from ..citationreport import render_citation_report
from .factory import load_config, build_runner

_DEFAULT_CONFIG = Path(".warrant/api-config.json")
_INIT_OUTPUT = Path(".warrant/api-config.json")


def cmd_run(args: argparse.Namespace) -> None:
    config_path = Path(args.config)
    config = load_config(config_path)
    Path(config.out_dir).mkdir(parents=True, exist_ok=True)
    runner = build_runner(config)
    run_state, report = runner.run(args.direction)
    print(render_citation_report(report))
    print(f"worktree: {run_state.worktree_path}")


def cmd_resume(args: argparse.Namespace) -> None:
    config_path = Path(args.config)
    config = load_config(config_path)
    run_state = runstore.load_latest_run(Path(config.out_dir))
    runner = build_runner(config)
    run_state, report = runner.resume(run_state)
    print(render_citation_report(report))
    print(f"worktree: {run_state.worktree_path}")


def cmd_init(args: argparse.Namespace) -> None:
    out_path = _INIT_OUTPUT
    if out_path.exists():
        answer = input(f"{out_path} already exists. Overwrite? [y/N]: ").strip().lower()
        if answer != "y":
            print("Aborted.")
            return
    base_repo = input("Base repo path [.]: ").strip() or "."
    index_path = input("Index path [sample-library/index]: ").strip() or "sample-library/index"
    out_dir = input("Output directory [.warrant/runs]: ").strip() or ".warrant/runs"
    out_path.parent.mkdir(parents=True, exist_ok=True)
    out_path.write_text(
        json.dumps({"index_path": index_path, "base_repo": base_repo, "out_dir": out_dir}, indent=2),
        encoding="utf-8",
    )
    print(f"Config written to {out_path}")
    print('Next: warrant run --direction "..."')


def main() -> None:
    parser = argparse.ArgumentParser(
        prog="warrant",
        description="Warrant — book-grounded autonomous coding agent",
    )
    sub = parser.add_subparsers(dest="command", required=True)

    p_run = sub.add_parser("run", help="Start a new Warrant run")
    p_run.add_argument("--direction", required=True, help="What to build")
    p_run.add_argument(
        "--config",
        default=str(_DEFAULT_CONFIG),
        help="Path to config JSON (default: .warrant/api-config.json)",
    )
    p_run.set_defaults(func=cmd_run)

    p_resume = sub.add_parser("resume", help="Resume the latest run")
    p_resume.add_argument(
        "--config",
        default=str(_DEFAULT_CONFIG),
        help="Path to config JSON (default: .warrant/api-config.json)",
    )
    p_resume.set_defaults(func=cmd_resume)

    p_init = sub.add_parser("init", help="Scaffold .warrant/api-config.json interactively")
    p_init.set_defaults(func=cmd_init)

    args = parser.parse_args()
    try:
        args.func(args)
    except Exception as exc:
        print(f"error: {exc}", file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run all API tests**

```bash
python -m pytest loop/tests/test_api_init.py loop/tests/test_api_factory.py loop/tests/test_api_invokers.py loop/tests/test_api_materializer.py -v
```

Expected: all pass. (`test_main_run_calls_runner` in `test_api_factory.py` explicitly passes `--config`, so the default change does not break it.)

- [ ] **Step 5: Commit**

```bash
git add loop/loop/api/__main__.py loop/tests/test_api_init.py
git commit -m "feat: add warrant init subcommand; fix default config path to api-config.json"
```

---

## Task 2: Sample library data and build script

**Files:**
- Create: `sample-library/principles.json`
- Create: `sample-library/build_index.py`

### Context

`librarian/librarian/models.py` defines `Principle(id, statement, citation, checkability_tier, evidence_chunk)` and `Citation(book, isbn, chapter, section)`. `store.save_index(index, out_dir)` writes `principles/`, `embeddings.npy`, `edges.json`, `manifest.json` — pure numpy + JSON, no chromadb. `edges.extract_edges` requires an LLM; the build script instead derives edges directly from `related_ids` in the JSON (using `"shares_topic"` kind), so no API key is needed at build time.

`related_ids` is not a `Principle` field. The build script pops it from each dict before constructing `Principle` objects.

### Steps

- [ ] **Step 1: Create `sample-library/principles.json`**

```json
[
  {
    "id": "yagni",
    "statement": "Build only what the current requirement demands; defer everything else.",
    "citation": {"book": "Agile Software Development Principles", "isbn": "", "chapter": "Design Principles", "section": "YAGNI"},
    "checkability_tier": 1,
    "evidence_chunk": "Adding a feature before it is needed costs twice: once to build it and once to refactor it when the real requirement arrives.",
    "related_ids": ["kiss", "dry"]
  },
  {
    "id": "kiss",
    "statement": "Prefer the simplest design that solves the problem; complexity is a liability.",
    "citation": {"book": "The Pragmatic Programmer", "isbn": "", "chapter": "A Pragmatic Approach", "section": "KISS"},
    "checkability_tier": 2,
    "evidence_chunk": "Every layer of abstraction added speculatively must be maintained, understood, and debugged without the benefit of a concrete use case to guide it.",
    "related_ids": ["yagni", "small-focused-units"]
  },
  {
    "id": "dry",
    "statement": "Every piece of knowledge must have a single, authoritative representation in the system.",
    "citation": {"book": "The Pragmatic Programmer", "isbn": "", "chapter": "A Pragmatic Approach", "section": "DRY"},
    "checkability_tier": 1,
    "evidence_chunk": "Duplication of logic or data leads to divergence: when you change one copy, the others silently drift, producing contradictions the compiler cannot catch.",
    "related_ids": ["yagni", "srp"]
  },
  {
    "id": "explicit-over-implicit",
    "statement": "Make behavior visible in code; implicit magic is technical debt.",
    "citation": {"book": "Clean Code", "isbn": "", "chapter": "Meaningful Names", "section": "Explicit Intent"},
    "checkability_tier": 2,
    "evidence_chunk": "Code that relies on hidden conventions requires the reader to hold a mental model of side-effects that are not visible at the call site.",
    "related_ids": ["defensive-programming", "fail-fast"]
  },
  {
    "id": "fail-fast",
    "statement": "Detect and report errors at the earliest possible point rather than continuing with invalid state.",
    "citation": {"book": "Release It!", "isbn": "", "chapter": "Stability Patterns", "section": "Fail Fast"},
    "checkability_tier": 1,
    "evidence_chunk": "Systems that fail fast surface bugs at the point of introduction; systems that fail slow propagate corruption through the call stack, making root cause analysis expensive.",
    "related_ids": ["design-for-failure", "defensive-programming", "explicit-over-implicit"]
  },
  {
    "id": "defensive-programming",
    "statement": "Validate all inputs at system boundaries; trust nothing from outside your module.",
    "citation": {"book": "Code Complete", "isbn": "", "chapter": "Defensive Programming", "section": "Input Validation"},
    "checkability_tier": 1,
    "evidence_chunk": "Defensive programming assumes that callers will pass incorrect data; validation at boundaries prevents errors from propagating deep into the system where their origin is obscured.",
    "related_ids": ["fail-fast", "design-for-failure"]
  },
  {
    "id": "design-for-failure",
    "statement": "Assume components will fail and design explicit recovery paths for each failure mode.",
    "citation": {"book": "Designing Distributed Systems", "isbn": "", "chapter": "Reliability", "section": "Failure Modes"},
    "checkability_tier": 2,
    "evidence_chunk": "In distributed systems, partial failure is the norm. A service without explicit failure handling treats all failures as catastrophic, preventing graceful degradation.",
    "related_ids": ["fail-fast", "design-for-observability", "rollback-over-fix-forward"]
  },
  {
    "id": "tdd",
    "statement": "Write a failing test before writing the code that makes it pass.",
    "citation": {"book": "Test-Driven Development By Example", "isbn": "", "chapter": "TDD Patterns", "section": "Red-Green-Refactor"},
    "checkability_tier": 1,
    "evidence_chunk": "Writing the test first forces you to define the interface before the implementation, producing APIs shaped by the caller rather than by the implementer.",
    "related_ids": ["test-isolation", "fail-fast"]
  },
  {
    "id": "test-isolation",
    "statement": "Each test must set up its own state and tear it down, with no shared mutable state across tests.",
    "citation": {"book": "xUnit Test Patterns", "isbn": "", "chapter": "Test Smells", "section": "Interacting Tests"},
    "checkability_tier": 1,
    "evidence_chunk": "Tests that share state create order dependencies: a test that passes in isolation may fail when run after another, making the suite non-deterministic.",
    "related_ids": ["tdd", "mock-at-boundaries"]
  },
  {
    "id": "mock-at-boundaries",
    "statement": "Mock only at system boundaries such as databases and HTTP; never mock internals you own.",
    "citation": {"book": "Growing Object-Oriented Software Guided by Tests", "isbn": "", "chapter": "Listening to the Tests", "section": "Mocking"},
    "checkability_tier": 2,
    "evidence_chunk": "Mocking internal collaborators creates tests that verify implementation detail rather than behavior, making any refactoring that preserves behavior appear to break tests.",
    "related_ids": ["test-isolation", "dependency-inversion"]
  },
  {
    "id": "srp",
    "statement": "Each module or class should have exactly one reason to change.",
    "citation": {"book": "Clean Architecture", "isbn": "", "chapter": "Design Principles", "section": "Single Responsibility Principle"},
    "checkability_tier": 2,
    "evidence_chunk": "A module with two responsibilities will be changed by two different stakeholders; their independent edits will interfere, creating unexpected coupling.",
    "related_ids": ["dry", "small-focused-units", "dependency-inversion"]
  },
  {
    "id": "dependency-inversion",
    "statement": "High-level modules must not depend on low-level modules; both should depend on abstractions.",
    "citation": {"book": "Clean Architecture", "isbn": "", "chapter": "Design Principles", "section": "Dependency Inversion Principle"},
    "checkability_tier": 2,
    "evidence_chunk": "Direct dependency on concrete implementations couples modules at the source level, making it impossible to substitute implementations without modifying callers.",
    "related_ids": ["srp", "mock-at-boundaries"]
  },
  {
    "id": "small-focused-units",
    "statement": "Keep functions, classes, and modules small enough to be understood in a single reading.",
    "citation": {"book": "Clean Code", "isbn": "", "chapter": "Functions", "section": "Small Functions"},
    "checkability_tier": 1,
    "evidence_chunk": "Small units can be named precisely, tested in isolation, and understood without requiring context from surrounding code; large units accumulate hidden responsibilities.",
    "related_ids": ["srp", "kiss"]
  },
  {
    "id": "design-for-observability",
    "statement": "Emit structured logs, metrics, and traces from the start; you cannot debug what you cannot observe.",
    "citation": {"book": "Observability Engineering", "isbn": "", "chapter": "Principles", "section": "Observability First"},
    "checkability_tier": 2,
    "evidence_chunk": "Systems without observability force engineers to reproduce failures locally; with observability you can understand any system state from production telemetry alone.",
    "related_ids": ["design-for-failure", "rollback-over-fix-forward"]
  },
  {
    "id": "rollback-over-fix-forward",
    "statement": "When a deployment causes an incident, roll back to the last known-good state before debugging.",
    "citation": {"book": "Accelerate", "isbn": "", "chapter": "Technical Practices", "section": "Deployment Practices"},
    "checkability_tier": 3,
    "evidence_chunk": "Fix-forward prolongs outages because debugging under time pressure in a degraded system produces unreliable conclusions; rollback restores service and gives a clean environment.",
    "related_ids": ["design-for-observability", "design-for-failure"]
  }
]
```

- [ ] **Step 2: Create `sample-library/build_index.py`**

```python
#!/usr/bin/env python3
"""Build sample-library/index from principles.json.

Run once during development from the repo root:
    python sample-library/build_index.py

Output (sample-library/index/) is committed to git. Users never run this.
"""
from __future__ import annotations
import json
from pathlib import Path

HERE = Path(__file__).parent


def main() -> None:
    raw: list[dict] = json.loads((HERE / "principles.json").read_text(encoding="utf-8"))

    # Pop related_ids before building Principle objects (not a model field)
    related: dict[str, list[str]] = {p["id"]: p.pop("related_ids", []) for p in raw}

    from librarian.models import Principle, Citation, Edge
    from librarian.store import Index, save_index
    from librarian.embedding import Embedder

    principles = [
        Principle(
            id=p["id"],
            statement=p["statement"],
            citation=Citation(**p["citation"]),
            checkability_tier=p["checkability_tier"],
            evidence_chunk=p["evidence_chunk"],
        )
        for p in raw
    ]

    print(f"Embedding {len(principles)} principles...")
    embedder = Embedder()
    embeddings = embedder.encode([p.statement for p in principles])

    # Build edges from related_ids; use shares_topic (no LLM needed)
    id_set = {p.id for p in principles}
    edges: list[Edge] = []
    seen: set[tuple[str, str]] = set()
    for p in principles:
        for dst_id in related.get(p.id, []):
            if dst_id not in id_set or dst_id == p.id:
                continue
            key = (min(p.id, dst_id), max(p.id, dst_id))
            if key in seen:
                continue
            seen.add(key)
            edges.append(Edge(src=p.id, dst=dst_id, kind="shares_topic"))

    index = Index(principles=principles, embeddings=embeddings, edges=edges)
    out_dir = HERE / "index"
    save_index(index, out_dir)
    print(f"Saved {len(principles)} principles, {len(edges)} edges → {out_dir}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: Verify the build script is importable (dry-run)**

```bash
python -c "import sample-library.build_index"
```

If this fails (sample-library is not a package), that is expected — it is a script, not a package. Skip this step.

Instead verify syntax:

```bash
python -m py_compile sample-library/build_index.py
echo $?  # Expected: 0
```

- [ ] **Step 4: Commit data + script**

```bash
git add sample-library/principles.json sample-library/build_index.py
git commit -m "feat: sample-library principles.json and build_index.py"
```

---

## Task 3: Build the index and create `demo-config.json`

**Files:**
- Create: `sample-library/index/` (generated, then committed)
- Create: `sample-library/demo-config.json`

### Context

Running `build_index.py` from the repo root calls `librarian.embedding.Embedder` (sentence-transformers) and `librarian.store.save_index`. Output: `sample-library/index/principles/`, `sample-library/index/embeddings.npy`, `sample-library/index/edges.json`, `sample-library/index/manifest.json`. All four files must be committed.

`demo-config.json` is used by `make demo`. It points at this index with reduced caps so the demo finishes in under a minute.

### Steps

- [ ] **Step 1: Install packages**

```bash
pip install -e "loop/[api]" -e librarian/ -e agent/
```

Expected: no errors. (sentence-transformers is a dependency of librarian.)

- [ ] **Step 2: Run the build script**

```bash
python sample-library/build_index.py
```

Expected output:
```
Embedding 15 principles...
Saved 15 principles, N edges → sample-library/index
```

where N is the number of unique related_id pairs (approximately 15–20).

- [ ] **Step 3: Verify index files exist**

```bash
ls sample-library/index/
```

Expected:
```
edges.json  embeddings.npy  manifest.json  principles/
```

```bash
ls sample-library/index/principles/ | wc -l
```

Expected: `15`

```bash
python -c "
from librarian.store import load_index
idx = load_index('sample-library/index')
print(len(idx.principles), 'principles')
print(idx.embeddings.shape)
print(len(idx.edges), 'edges')
"
```

Expected: `15 principles`, shape `(15, 384)`, edges count matching the build output.

- [ ] **Step 4: Create `sample-library/demo-config.json`**

```json
{
  "index_path": "sample-library/index",
  "base_repo": ".",
  "out_dir": ".warrant/demo-runs",
  "global_iteration_cap": 3,
  "per_node_attempt_cap": 2,
  "verify_iteration_cap": 1
}
```

- [ ] **Step 5: Verify demo config loads**

```bash
python -c "
from loop.api.factory import load_config
cfg = load_config('sample-library/demo-config.json')
print(cfg.index_path, cfg.global_iteration_cap)
"
```

Expected: `sample-library/index 3`

- [ ] **Step 6: Commit all**

```bash
git add sample-library/index/ sample-library/demo-config.json
git commit -m "feat: pre-built sample-library index and demo-config.json"
```

---

## Task 4: Makefile

**Files:**
- Create: `Makefile`

### Context

Makefile lines must use a tab character for indentation, not spaces. Four targets: `install`, `install-api`, `test`, `demo`. `make demo` requires `ANTHROPIC_API_KEY` to be set (it runs the live agent). `make test` runs unit tests only (no API key needed).

### Steps

- [ ] **Step 1: Write the Makefile**

Create `Makefile` at the repo root:

```makefile
.PHONY: install install-api test demo

install:
	pip install -e loop/ -e librarian/ -e agent/

install-api:
	pip install -e "loop/[api]" -e librarian/ -e agent/

test:
	python -m pytest loop/tests/ -q
	python -m pytest agent/tests/ -q
	python -m pytest librarian/tests/ -q

demo:
	warrant run \
		--config sample-library/demo-config.json \
		--direction "add a hello_world function that prints Hello from Warrant"
```

**Important:** Every recipe line must start with a literal tab character (ASCII 0x09), not spaces. Most editors default to spaces — verify with `cat -A Makefile | grep '^\^I'` or similar.

- [ ] **Step 2: Verify `make test` runs**

```bash
make test
```

Expected: all test suites pass. If you see `Makefile:N: *** missing separator.  Stop.`, the indentation is spaces, not tabs. Replace with tabs.

- [ ] **Step 3: Verify `make install-api` runs**

```bash
make install-api
```

Expected: pip installs successfully (packages already installed, so this is a no-op or a quick check).

- [ ] **Step 4: Commit**

```bash
git add Makefile
git commit -m "feat: Makefile with install, install-api, test, demo targets"
```

---

## Task 5: README.md

**Files:**
- Create: `README.md`

### Context

README is the first thing a visitor reads on GitHub. It must convey what Warrant does, show both modes (skill and CLI), and get a new user running in under 10 minutes. Configuration field table matches `ApiConfig` in `loop/loop/api/factory.py` exactly. No filler sections.

### Steps

- [ ] **Step 1: Write `README.md`**

Create `README.md` at the repo root:

```markdown
# Warrant

Warrant is a book-grounded autonomous coding agent. Given a single direction, it runs an Orient → Retrieve → Plan → Execute → Verify loop, grounding every plan step in engineering principles drawn from a Librarian index. Plans cite their sources; the Verify phase checks them.

## What it does

- **Orient** — decomposes the direction into a plan tree of independent tasks
- **Retrieve** — pulls relevant engineering principles from the index for each task
- **Plan** — annotates each node with applicable checks drawn from retrieved principles
- **Execute** — runs each task in an isolated git worktree, calling real tools
- **Verify** — checks the executor's output against the plan's cited principles

Two modes: `/warrant <direction>` as a Claude Code skill, or `warrant run --direction "..."` as a standalone CLI using the Anthropic API directly.

## Quick start (CLI mode)

```bash
export ANTHROPIC_API_KEY=sk-ant-...
make install-api
warrant init
warrant run --direction "add pagination to the user list endpoint"
```

`warrant init` scaffolds `.warrant/api-config.json` with three prompts (repo path, index path, output dir). The defaults point at `sample-library/`, a pre-built index bundled with the repo, so you can run immediately without building your own.

## Prerequisites

- Python 3.11+
- git
- `ANTHROPIC_API_KEY` environment variable (CLI mode only)

## Installation

### Skill mode (Claude Code)

```bash
pip install -e loop/ -e librarian/ -e agent/
cp .warrant/config.example.json .warrant/config.json
# Edit config.json: set index_path and base_repo
```

Then invoke `/warrant <direction>` in any Claude Code session where `.warrant/config.json` exists. See `SKILL.md` for details.

### CLI mode (Anthropic API)

```bash
make install-api          # or: pip install -e "loop/[api]" -e librarian/ -e agent/
warrant init              # writes .warrant/api-config.json interactively
warrant run --direction "..."
```

To resume the most recent run if it was interrupted:

```bash
warrant resume
```

## Configuration

### CLI: `.warrant/api-config.json`

| Field | Default | Description |
|---|---|---|
| `index_path` | required | Path to the Librarian index directory |
| `base_repo` | required | Git repo Warrant operates on |
| `out_dir` | `.warrant/runs` | Where run artifacts (plans, run state) are stored |
| `model_name` | `sentence-transformers/all-MiniLM-L6-v2` | Embedding model for retrieval |
| `reranker_name` | `cross-encoder/ms-marco-MiniLM-L-6-v2` | Reranker model for retrieval |
| `claude_model` | `claude-sonnet-4-6` | Anthropic model for the executor agent |
| `claude_model_verifier` | `claude-sonnet-4-6` | Anthropic model for the verifier agent |
| `max_tool_rounds` | `50` | Max tool calls per agent turn |
| `global_iteration_cap` | `10` | Max global loop iterations |
| `per_node_attempt_cap` | `3` | Max attempts per plan node before marking failed |
| `watchdog_timeout` | `300.0` | Seconds before a node execution is timed out |
| `max_parallel` | `3` | Max plan nodes executing simultaneously |
| `max_principles` | `15` | Max principles retrieved per query |
| `verify_iteration_cap` | `3` | Max verifier iterations per node |

`ANTHROPIC_API_KEY` is read from the environment; it is never stored in the config file.

### Skill: `.warrant/config.json`

See `.warrant/config.example.json` for all fields. Same structure as above minus the Anthropic API options.

## Sample library

`sample-library/` contains a pre-built Librarian index with 15 engineering principles covering design, robustness, testing, architecture, and operations. It is ready to use out of the box — `warrant init` defaults to it.

To build your own index from O'Reilly books (requires the `colophon` CLI and a book library):

```bash
librarian index <path-to-book-library> --output <path-to-index>
```

Then set `index_path` in your config to `<path-to-index>`.

## Repository structure

```
agent/          Plan artifact: versioned decision-tree data structure
librarian/      Librarian: indexes engineering principles into a HybridRAG index
loop/           Agent loop: Orient → Retrieve → Plan → Execute → Verify
loop/loop/skill/    Claude Code skill wrapper
loop/loop/api/      Standalone CLI (Anthropic API)
sample-library/ Pre-built index with 15 engineering principles
SKILL.md        Claude Code skill entry point
.warrant/       Config and run artifacts (gitignored except examples)
```

## License

MIT
```

- [ ] **Step 2: Verify the README renders correctly**

```bash
python -c "
import re
text = open('README.md').read()
# Check all section headers are present
headers = ['Quick start', 'Prerequisites', 'Installation', 'Configuration', 'Sample library', 'Repository structure', 'License']
for h in headers:
    assert h in text, f'Missing section: {h}'
print('All sections present')
"
```

Expected: `All sections present`

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "feat: README.md — project face with quick start, config reference, structure"
```

---

## Final verification

After all tasks are done:

- [ ] **Run full test suite**

```bash
python -m pytest loop/tests/ agent/tests/ librarian/tests/ --tb=short -q
```

Expected: all tests pass (249+ total; Task 1 added 8 new tests in `test_api_init.py`).

- [ ] **Verify `warrant init` is registered as a subcommand**

```bash
warrant --help
```

Expected output includes `init` in the list of subcommands:

```
{run,resume,init}
```

- [ ] **Verify `make test` passes**

```bash
make test
```

Expected: all three test suites pass without errors.

- [ ] **Verify sample index loads**

```bash
python -c "
from librarian.store import load_index
idx = load_index('sample-library/index')
assert len(idx.principles) == 15
assert idx.embeddings.shape == (15, 384)
print('OK:', len(idx.principles), 'principles,', len(idx.edges), 'edges')
"
```

Expected: `OK: 15 principles, N edges` (N >= 10).
